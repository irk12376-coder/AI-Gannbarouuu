# 経理自動化室(マルチエージェント組織)

このリポジトリは、社内の事務的業務をエージェント(サブエージェント)に分担させて自動化するための「組織」です。Claude Code のセッションが「統括(オーケストレーター)」として、業務内容に応じて適切な担当エージェントに処理を委譲(Agent ツール)します。

## 組織図

```
統括(このセッション自身)
├─ expense-report-processor … 経費精算担当
└─ invoice-processor        … 請求書処理担当
```

今後、業務範囲を段階的に広げる際は、この組織図とメンバー一覧に新しい担当エージェントを追加していきます(下記「拡張方法」参照)。

## メンバー一覧

| 担当 | 定義ファイル | 入力 | 出力 | 参照ルール |
|---|---|---|---|---|
| 経費精算担当 | `.claude/agents/expense-report-processor.md` | `inbox/expense/` | `processed/expense/*.csv` | `rules/expense-policy.yaml` |
| 請求書処理担当 | `.claude/agents/invoice-processor.md` | `inbox/invoice/` | `processed/invoice/*.csv` | `rules/invoice-policy.yaml` |

## 使い方

1. 処理したいファイル(領収書・請求書など)を対応する `inbox/<業務>/` に置く。
2. `/process-expenses` または `/process-invoices` を実行する。あるいは単に「経費精算して」「請求書処理して」と依頼すれば、統括が該当エージェントに委譲する。
3. 結果は `processed/<業務>/` にCSVで出力される。ステータスが `要確認` / `規定違反` / `重複疑い` の行は人間が必ず確認する。
4. 元ファイルは `processed/<業務>/originals/` に自動で退避される。

**重要**: 各エージェントは一次チェックとデータ整理のみを行い、会計システムへの実際の登録・振込・支払実行は一切行いません。最終判断と実行は必ず人間が行ってください。

## 拡張方法(新しい業務を追加する)

事務作業を新しく自動化する際は、次の手順でこの組織にメンバー(エージェント)を追加します。

1. `.claude/agents/` に新しいサブエージェント定義(`.md`)を作成する。既存の `expense-report-processor.md` を雛形にすると早い。`description` には「いつこのエージェントを使うべきか」を具体的に書く(統括が自動判断する材料になる)。
2. 必要なら `rules/<業務>-policy.yaml` に判断基準(規定・閾値)を定義する。ハードコードせず、規定はここに集約する。
3. `inbox/<業務>/` と `processed/<業務>/originals/` を作成する。
4. `.claude/commands/process-<業務>.md` にショートカットコマンドを追加する(任意)。
5. 本ファイルの「組織図」「メンバー一覧」を更新する。

## 設計方針

- 各エージェントは判断に迷う場合、勝手に決めつけず「要確認」を出す(誤った自動処理より、人間へのエスカレーションを優先)。
- 規定・閾値はコードでなく `rules/*.yaml` に外出しし、非エンジニアでも編集できるようにする。
- 個人情報・口座情報などの機微情報は出力CSVに極力残さない。
