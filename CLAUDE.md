# がんばろう株式会社(マルチエージェント組織)

このリポジトリは、社内の事務的業務をエージェント(サブエージェント)に分担させて自動化するための「会社」です。Claude Code のセッションが「経営統括室」として、業務内容に応じて適切な部門のエージェントに処理を委譲(Agent ツール)します。会社の様子は `webapp/` でビジュアル(オフィス画面)としても確認できます。

## 組織図

```
社長(あなた)
  │
経営統括室(このセッション自身。各部門への指示出し・進捗の取りまとめ)
  │
  ├─ 経理部 [稼働中]
  │    ├─ expense-report-processor … 経費精算担当
  │    └─ invoice-processor        … 請求書処理担当
  │
  ├─ X運用部 [開設予定]
  ├─ インスタ運用部 [開設予定]
  └─ YouTube運用部 [開設予定]
```

正式な組織定義は `company/org.json` にあります。組織図・メンバー一覧を更新したら必ずこのファイルも合わせて更新してください(`webapp/` のオフィス画面はこのファイルを読んで自動描画されます)。

今後、業務範囲を段階的に広げる際は、この組織図と `company/org.json` に新しい部門・担当エージェントを追加していきます(下記「拡張方法」参照)。SNS運用部門(X/インスタ/YouTube)は将来的に完全自動投稿を目指す方針ですが、実際のAPI連携は行っておらず、現時点では組織の器(役職定義)のみ用意しています。

## メンバー一覧

| 部門 | 担当 | 定義ファイル | 入力 | 出力 | 参照ルール |
|---|---|---|---|---|---|
| 経理部 | 経費精算担当 | `.claude/agents/expense-report-processor.md` | `inbox/expense/` | `processed/expense/*.csv` | `rules/expense-policy.yaml` |
| 経理部 | 請求書処理担当 | `.claude/agents/invoice-processor.md` | `inbox/invoice/` | `processed/invoice/*.csv` | `rules/invoice-policy.yaml` |

## 使い方

1. 処理したいファイル(領収書・請求書など)を対応する `inbox/<業務>/` に置く。
2. `/process-expenses` または `/process-invoices` を実行する。あるいは単に「経費精算して」「請求書処理して」と依頼すれば、経営統括室が該当エージェントに委譲する。
3. 結果は `processed/<業務>/` にCSVで出力される。ステータスが `要確認` / `規定違反` / `重複疑い` の行は人間が必ず確認する。
4. 元ファイルは `processed/<業務>/originals/` に自動で退避される。
5. `python3 -m http.server 8000` をリポジトリルートで起動し `http://localhost:8000/webapp/` を開くと、稼働中のエージェントがオフィスで動いている様子を見られる(詳細は `webapp/README.md`)。

**重要**: 各エージェントは一次チェックとデータ整理のみを行い、会計システムへの実際の登録・振込・支払実行は一切行いません。最終判断と実行は必ず人間が行ってください。

## 拡張方法(新しい部門・業務を追加する)

事務作業を新しく自動化する際は、次の手順でこの会社に部門・メンバー(エージェント)を追加します。

1. `.claude/agents/` に新しいサブエージェント定義(`.md`)を作成する。既存の `expense-report-processor.md` を雛形にすると早い。`description` には「いつこのエージェントを使うべきか」を具体的に書く(経営統括室が自動判断する材料になる)。作業の開始・終了時に `company/status.json` の自分のエントリを更新する手順も忘れずに含める(既存エージェントの「ステータス連携」セクションを参照)。
2. 必要なら `rules/<業務>-policy.yaml` に判断基準(規定・閾値)を定義する。ハードコードせず、規定はここに集約する。
3. `inbox/<業務>/` と `processed/<業務>/originals/` を作成する。
4. `.claude/commands/process-<業務>.md` にショートカットコマンドを追加する(任意)。
5. `company/org.json` に部門・メンバーを追加し(`status` を `"active"` にする)、`company/status.json` にも同じ `id` でエントリを追加する。
6. 本ファイルの「組織図」「メンバー一覧」を更新する。

## 設計方針

- 各エージェントは判断に迷う場合、勝手に決めつけず「要確認」を出す(誤った自動処理より、人間へのエスカレーションを優先)。
- 規定・閾値はコードでなく `rules/*.yaml` に外出しし、非エンジニアでも編集できるようにする。
- 個人情報・口座情報などの機微情報は出力CSVに極力残さない。
- 外部SNS等への実際の投稿・送信のように取り返しがつかない/社外に公開される操作を行うエージェントは、本人が下書きを審査できる仕組み(部門長による承認フロー)を必ず備えてから実装する。API連携時は認証情報をリポジトリにコミットしない。
